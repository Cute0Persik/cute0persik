
  (function($) {  
    $(function() {  
    $('nav ul li a:not(:only-child)').click(function(e) {
    $(this).siblings('.sequel-dropdown').toggle();
    $('.dropdown').not($(this).siblings()).hide();
    e.stopPropagation();
    });
    $('html').click(function() {
    $('.sequel-dropdown').hide();
    });
    $('#menu-button').click(function() {
    $('ul').slideToggle();
    });
    $('#menu-button').on('click', function() {
    this.classList.toggle('desamuvsnis');
    });
    });  
  })(jQuery);

(
  function(){
    var modal = document.querySelector('.modal-container');
    var closeButton = document.querySelector('.close');
    var modalTriggers = document.querySelectorAll('[data-trigger]');

  
    var isModalOpen = false;
    var pageYOffset = 0;
  
    var openModal = function() {
      pageYOffset = window.pageYOffset;
      modal.classList.add('is-open');
      isModalOpen = true;
    }
  
    var closeModal = function() {
      modal.classList.remove('is-open');
      isModalOpen = false;
    }
  
    var onScroll = function(e) {
      if (isModalOpen) {
        e.preventDefault();
        window.scrollTo(0, pageYOffset);
      }
    }
  
    modalTriggers.forEach(function(item) { 
      item.addEventListener('click', openModal);
    })
  
    document.addEventListener('scroll', onScroll);
  
    closeButton.addEventListener('click', closeModal);
  })();

  (function(){
    var modal = document.querySelector('.modal-container2');
    var closeButton = document.querySelector('.close2');
    var modalTriggers = document.querySelectorAll('[data-trigger2]');

  
    var isModalOpen = false;
    var pageYOffset = 0;
  
    var openModal = function() {
      pageYOffset = window.pageYOffset;
      modal.classList.add('is-open2');
      isModalOpen = true;
    }
  
    var closeModal = function() {
      modal.classList.remove('is-open2');
      isModalOpen = false;
    }
  
    var onScroll = function(e) {
      if (isModalOpen) {
        e.preventDefault();
        window.scrollTo(0, pageYOffset);
      }
    }
  
    modalTriggers.forEach(function(item) { 
      item.addEventListener('click', openModal);
    })
  
    document.addEventListener('scroll', onScroll);
  
    closeButton.addEventListener('click', closeModal);
  })();
  
  (function(){
    var modal = document.querySelector('.modal-container3');
    var closeButton = document.querySelector('.close3');
    var modalTriggers = document.querySelectorAll('[data-trigger3]');

  
    var isModalOpen = false;
    var pageYOffset = 0;
  
    var openModal = function() {
      pageYOffset = window.pageYOffset;
      modal.classList.add('is-open3');
      isModalOpen = true;
    }
  
    var closeModal = function() {
      modal.classList.remove('is-open3');
      isModalOpen = false;
    }
  
    var onScroll = function(e) {
      if (isModalOpen) {
        e.preventDefault();
        window.scrollTo(0, pageYOffset);
      }
    }
  
    modalTriggers.forEach(function(item) { 
      item.addEventListener('click', openModal);
    })
  
    document.addEventListener('scroll', onScroll);
  
    closeButton.addEventListener('click', closeModal);
  })();





